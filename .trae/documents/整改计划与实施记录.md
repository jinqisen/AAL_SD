# AAL-SD整改计划与实施记录

## 目标
对齐实现与设计文档，修复Agent协议、提示模板、数据路径与控制器依赖的不一致，并保证严格恢复与跑批可审计。

## 整改计划
1. 对齐配置与数据路径，兼容现有数据目录
2. 统一Agent工具返回协议与Prompt模板
3. 修复Agent工具依赖与主动学习流程数据绑定
4. 统一基线采样器输出结构并更新主流程
5. 完成验证并记录结果

## 实施记录
- 配置与数据路径：已对齐并保留旧路径回退
- Agent协议与Prompt模板：已对齐System Prompt与工具返回结构
- 控制器依赖：已补齐dataset绑定与训练状态注入
- 基线采样器输出：已统一为字典结构
- 验证：见本次执行记录

## 实施记录（2026-01-31）
- LLM配置：支持本机私有路径配置并移除仓库内明文Key
- Agent回退：记录topk回退统计并纳入报告
- 测试：新增私有配置Key读取单测
- 实验执行：修复实验报告保存目录初始化

## 实施记录（2026-02-01）
- Agent解析健壮性：兼容Action全角冒号、JSON尾随文本与代码块包装
- Agent稳定性：InvalidAction时追加格式纠正提示，降低Missing Action连发概率
- SiliconFlow stop序列：改为\nObservation避免输出被截断为空
- 测试：新增Missing Action后恢复的回归用例
- LLM超时策略：新增指数退避重试，超时后可配置中止实验/中止全量实验
- LLM配置健壮性：修复布尔配置解析并清理默认明文Key
- 实验续跑策略：优先恢复最早未完成的日志实例（已在2026-02-02调整为恢复最新）

## 实施记录（2026-02-02）
- 实验续跑策略：恢复最新未完成日志，避免日志与pool状态错位
- Pools与Checkpoint：按run_id隔离，避免跨次运行污染对比
- 选样更新：未标注不足时允许partial selection并提前结束实验
- Agent容错：LLM失败时可配置回退到非Agent采样，避免中断整组实验

## 实施记录（2026-02-02 科研模式强化）
- 启动标志：支持fresh/resume模式，显式区分从头与断点继续
- 科研护栏：强制run_id，默认禁用legacy pools，严格resume缺失即失败
- 阶段报告：按epoch/round持续落盘status.json，暴露loss/指标异常与选样不足
- 复现性：数据文件列表排序与数据指纹写入manifest
- 路径一致性：统一results_dir与Config.RESULTS_DIR，保证manifest/status/trace同目录
- 续跑优先级：显式run_id时不再自动挪用最新未完成日志的run_id
- Fresh语义：fresh会重建该实验pools并清理该实验checkpoint/status/trace
- 数据目录探测：优先识别Landslide4Sense数据目录，避免误选CAS目录导致空数据

## 实施记录（2026-02-03）
- Prompt模板：移除专家人设，定位为动态控制器
- AD-KUCS逻辑：补充U/K定义、融合公式与lambda调整边界
- 决策约束：量化阈值与异常处理规则写入System Prompt

## 实施记录（2026-02-03 下午 - 算法严谨性与控制消融）
- AD-KUCS知识增益（K项）路径收敛：默认以 `coreset-to-labeled` 的特征距离覆盖度为K项，`clustering` 仅作为可选分枝/基线方法内部实现，不作为主方法默认路径。
- 冷启动/护栏：引入 `MIN_LABELED_FOR_NOVELTY` 阈值（5，见 `src/agent/config.py`），用于在已标注样本过少时限制“新颖性/代表性”相关决策，避免不稳定选样影响主结论。
- Agent控制权限：在 `Toolbox` 中集成细粒度权限检查 (`_check_permission`)，支持 `set_lambda`、`set_query_size`、`set_epochs_per_round` 的动态开关，服务于消融实验。
- 配置集中化：将Agent阈值与约束统一收敛到 `src/agent/config.py`（`AgentThresholds` / `AgentConstraints`）。

## 实施记录（2026-02-03 晚 - 回退可审计与降级护栏）

### 编码计划
1. 设计回退原因分型与trace字段schema，并写入主流程
2. 为AD-KUCS空输入/打分失败加入确定性降级策略，并在trace中显式标记
3. 在Toolbox层面让“无分数”仍可生成候选（带degraded标记），避免LLM/无Key路径卡死
4. 增补单测覆盖：score预计算失败不应抛异常、候选降级输出结构稳定

### 进展
- [已完成] 回退原因分型：main.py写入fallback_reason_type/fallback_policy并落trace与status
- [已完成] AD-KUCS空输入降级：当unlabeled_info为空或rank异常时走确定性hash排序并落trace
- [已完成] Toolbox降级候选：current_scores为空时get_top_k_samples仍返回degraded候选
- [已完成] 测试：新增Toolbox降级与score预计算失败用例，pytest全量通过

## 实施记录（2026-02-03 晚 - 科研护栏：创新点不弱化）

### 编码计划
1. 增加STRICT_INNOVATION等配置开关，明确科研模式下允许/禁止的回退
2. 在Agent回退与ranking降级发生时直接失败（但先落trace/status），避免结果被“混入退化轮次”
3. 增补单测覆盖严格模式触发失败

### 进展
- [已完成] 配置：新增STRICT_INNOVATION/FAIL_ON_AGENT_FALLBACK/FAIL_ON_RANKING_DEGRADED/REQUIRE_LLM_FOR_AGENT
- [已完成] 主流程：fallback或ranking_degraded在严格模式下抛错终止该实验
- [已完成] 测试：新增严格护栏触发用例，pytest全量通过
- [已完成] 严格跑批修复：补齐AgentThresholds.MIN_LABELED_FOR_NOVELTY，避免score_precalc_failed

## 实施记录（2026-02-06）

### 编码计划
1. 修复Agent单轮重复观测导致超步数、未提交finalize_selection的问题
2. 在AgentManager中加入权限/约束纠偏提示并强制最后一步提交
3. 强化Prompt对重复观测的限制并要求步数上限内提交
4. 增补回归测试覆盖重复观测与超步数分支

### 进展
- [已完成] AgentManager：max_steps对齐AgentConstraints；限制观测工具每轮最多2次；最后一步强制提示finalize_selection；超步数返回AgentMaxSteps；对PermissionDenied/ConstraintViolation追加纠偏提示
- [已完成] Prompt模板：限制重复观测并强调步数上限内必须提交
- [已完成] 约束参数：MAX_STEPS默认调整为12
- [进行中] 测试：运行pytest并修复可能失败用例
- [已完成] 论文：修复paper Markdown中Figure图片链接为相对路径，确保渲染正常
- [已完成] 论文：按run_id=20260204_111154_strict汇总并插入Table 1主实验结果表

## 实施记录（2026-02-06 - 固定训练协议，避免epochs混杂）

### 编码计划
1. 将全局训练协议固定为每轮 epochs=10，并加护栏禁用动态epochs控制
2. 同步更新实验配置与论文叙述，明确动态训练预算调度作为后续工作
3. 运行pytest自检，确保改动不破坏现有测试

### 进展
- [已完成] 训练协议：Config默认EPOCHS_PER_ROUND=10，新增FIX_EPOCHS_PER_ROUND护栏
- [已完成] 主流程：禁用epoch schedule与pending epochs注入，确保resume不携带旧控制
- [已完成] 实验配置：full_model关闭set_epochs_per_round权限；epoch_schedule_from在固定协议下不生效
- [已完成] 论文：方法与实验设置改为“epochs固定=10”，移除预算诊断段落并将其留作后续版本内容
- [已完成] 测试：pytest全量通过

## 重新评估结果（2026-01-30）

### ADKUCSSampler实现验证
- `_normalize_scores`方法：已正确实现最大-最小归一化，处理分母为0的边缘情况
- `rank_samples`方法：返回结构符合设计文档要求，包含sample_id、final_score、uncertainty、knowledge_gain字段
- 自适应权重计算：`_get_adaptive_weight`方法正确实现Sigmoid函数

### AgentManager实现验证
- ReAct循环：正确实现Thought-Action-Observation循环，最大步数限制为12
- 交互协议：Action协议支持Tool Call和Final Answer两种JSON格式
- Observation协议：正确包装工具执行结果为{"status": "...", "result": "..."}结构
- 错误处理：支持ToolNotFound、InvalidParameters、ExecutionError等错误类型

### Toolbox工具集验证
- `get_top_k_samples`：返回结构包含id、U_score、K_score、final_score字段
- `get_sample_details`：返回样本详细元数据，包括U_score、K_score、image_name等
- `get_system_status`/`get_training_status`：返回训练状态与约束/预算相关信息
- `set_hyperparameter`：支持调整alpha参数
- `finalize_selection`：正确提交最终选择并更新控制器状态

### Prompt模板验证
- System Prompt：已更新为"GeoMind"角色，包含情境感知参数（T_max、t、lambda_t、current_miou）
- User Prompt：动态生成，包含当前迭代轮数和候选样本数量
- 工具定义：清晰描述每个工具的功能、参数和返回值

### 测试验证
- pytest测试通过：20个测试全部通过
- LLM Agent JSON序列化问题已修复：添加default=str参数
- 基线采样器测试：新增18个测试用例，覆盖Random、Entropy、Core-Set和ADKUCS四种采样器

### 基线测试覆盖（2026-01-30新增）
- RandomSampler测试：验证输出结构和空输入处理
- EntropySampler测试：验证熵计算、输出结构和缺失prob_map处理
- Core-Set Sampler测试：验证距离计算、输出结构、无标注特征处理和缺失特征处理
- ADKUCSSampler测试：验证归一化、不确定性计算、知识增益计算、自适应权重计算和rank_samples输出结构

### 结论
当前实现与设计文档基本对齐，所有核心功能已正确实现。系统可以正常运行主动学习流程。

## 实施记录（2026-02-05 - 严谨性整改与跑批可审计）
- 跑批完成判据：由日志尾部标记升级为status/ckpt优先判定，日志标记仅作为兜底

## 实施记录（2026-02-10 - coreset-to-labeled K 性能优化）
- coreset-to-labeled：保持 K=min_dist/max_pairwise_dist 逻辑不变，将 max_pairwise_dist 预计算并向量化 min_dist 计算
- 选样性能：避免每个样本重复 O(n_labeled^2) 计算导致长时间停滞
- 测试：新增等价性与 rank_samples 可运行性覆盖
- 实验清单：新增run级manifest，记录实验配置、Config快照与代码指纹
- 调度口径：将LLM/Non-LLM输出改为Agent/Non-Agent，避免命名误导
- 主方法优先：Agent队列中full_model前置，提升主结果迭代效率
- 结果影响评估：既有results维持不变，仅新增manifest与更稳定的完成判据

## 实施记录（2026-02-12 - full_model 默认策略合并）

### 编码计划
1. 将 full_model 直接切换为自适应阈值 rollback + coreset-to-labeled K + overfit_guard
2. 运行 pytest 与静态检查，确保变更不破坏既有测试

### 进展
- [已完成] 实验配置：full_model 默认启用 rollback_config(adaptive_threshold)、k_definition=coreset_to_labeled、overfit_guard
- [已完成] 验证：pytest 通过（ruff 未安装；mypy 存在大量既有类型错误）

## 实施记录（2026-02-12 - 单一路径化：移除 clustering/prev_drop 与 overfit_guard 开关）

### 编码计划
1. 移除 k_definition=clustering 与 rollback_mode=prev_drop 的分枝，仅保留 coreset-to-labeled + adaptive_threshold
2. 废除 overfit_guard 的可关闭逻辑，默认始终启用
3. 废除 labeled 为空时的聚类退化，改为直接抛错终止
4. 同步更新实验配置与测试，保证 pytest 全量通过

### 进展
- [已完成] 代码重构：清理分枝并统一默认路径
- [已完成] 严格性：labeled 为空时直接报错并终止
- [已完成] 验证：pytest 通过

## 实施记录（2026-02-21 - 策略判定与选样后处理配置化）

### 编码计划
1. 为训练状态引入可配置的mIoU信号（peak/EMA/last_k_mean）并保持默认不变
2. 为severe判定增加可配置逻辑（OR/AND）与TVC统计选择（min/last/mean/p10）
3. 新增Top-M候选的批内去冗余后处理与预测正样本面积软约束
4. 更新v4实验配置并验证对其他实验无影响

### 进展
- [已完成] 训练状态mIoU信号配置化
- [已完成] severe判定逻辑与统计配置化
- [已完成] Top-M去冗余与预测正样本面积约束
- [已完成] v4配置更新与回归验证

## 实施记录（2026-02-05 - control ablation 学术严谨性修订）
- 方案更新：补充alpha调参入口与排序lambda参数的权限闭环要求
- 默认lambda对齐：明确默认lambda必须与主流程计算逻辑一致
- 重跑范围：将agent_control_budget/agent_control_lambda/full_model标为论文场景必须重跑
- 复现要求：新增多随机种子与统计汇报要求
- run_id隔离：提示词/权限变更后需使用新run_id避免版本混杂

## 实施记录（2026-02-05 - control ablation 代码落地）
- Prompt权限注入：System Prompt显示control_permissions、允许动作与set_hyperparameter可用性
- 权限闭环：get_top_k_samples/get_candidate_samples在禁用set_lambda时锁定默认λ
- Alpha权限：set_hyperparameter按set_alpha/set_lambda权限控制
- 跑批修复：run_control_ablation动态读取agent_control_*配置

## 实施记录（2026-03-07 - 测试集闭环泄漏全链路整改）

### 改动说明
- 修复主动学习闭环在训练期使用 test/eval 集驱动策略（λ/回滚/风险）的泄漏风险
- 明确分离闭环验证集（V）与最终报告测试集（T），并加入强制护栏阻断回归
- 同步更新日志/trace/报告聚合口径，避免“验证集当测试集”写入论文产物

### 编码计划
1. 重构数据池：新增/规范 val_pool 与 test_pool 语义与生成逻辑
2. 重构训练主循环：闭环仅使用 val_loader，test 仅冻结后一次性评估
3. 增加科研护栏与回归测试：静态/运行时断言阻断 test-in-loop
4. 更新报告与分析脚本：显式区分 val 与 test 指标字段并避免混用

### 进展
- [进行中] 数据池与训练主循环的 V/T 分离整改
